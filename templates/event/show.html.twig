{% extends 'base.html.twig' %}
{% block title %}Event{% endblock %}
{% block body %}
    <h1>Event</h1>
    <div class="row">
        <div class="col-md-4">
            <img src="uploads/logo-wwf-2019.png" alt="West Web Festival">
        </div>
        <div class="col-md-8">
            <h1>{{ event.title }}</h1>
            <span class="badge badge-pill badge-secondary" data-toggle="tooltip" data-placement="top" title="Ville">
                        <i class="fa fa-map-pin"></i>
                        {{ event.city }}
                    </span>
            <span class="badge badge-pill badge-secondary" data-toggle="tooltip" data-placement="top" title="Dates">
                        <i class="fa fa-calendar"></i>
                        {{ event.dateStart|date('d') ~ '-' ~ event.dateEnd|date('d/m/Y')}}
                    </span>
            <span class="badge badge-pill badge-secondary" data-toggle="tooltip" data-placement="top"
                  title="Participants">
                        <i class="fa fa-users"></i>
                        16
                    </span>
            <a href="https://www.west-web-festival.fr" class="badge badge-pill badge-primary" data-toggle="tooltip"
               data-placement="top" title="Site internet">
                <i class="fa fa-globe"></i>
                {{ event.url }}
            </a>
            <span class="badge badge-pill badge-secondary" data-toggle="tooltip" data-placement="top" title="Prix">
                {% if event.price == 0 %}
                    Gratuit
                {% else %}
                    <i class="fa fa-euro"></i>
                    {{ event.price }}
                {% endif %}
                    </span>
            <p>{{ event.description}}</p>
            <a href="#" class="btn btn-primary btn-lg">J'y vais !</a>
        </div>
    </div>
{% endblock %}